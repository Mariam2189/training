(function() {
        //let elements = document.getElementsByTagName("p"),//NodeList
    //console.log(elements);
       // classElements = document.getElementsByClassName("paragraph"),
        //console.log(classElements);
        //idElements = document.getElementById("four"),
        //console.log(idElements);
        elementsSelector = document.querySelectorAll("p");
        console.log(elementsSelector);

    // for (let i = 0, len = elements.length; i < len; i++) {
    //     console.log(elements[i]);
    // }
    
})();